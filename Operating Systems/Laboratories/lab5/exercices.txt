1. Write a Shell script that executes all the
   commands received as command line
   parameters. Handle the case when these commands
   have their own command line arguments. Try
   using a separator.
	a.sh

2. Find all the files in /usr/bin that are not an
   executable binary.
	b.sh

3. Calculate the sum of the i-node IDs of the
   files in the current directory.
	c.sh
